# Registered portrait layers — Part 166

The approved portrait remains the stationary base. The mouth uses the existing
feathered local patch. The new eyes-closed.png supplies only two feathered eye
regions, leaving all other pixels untouched. BLINK.md preserves its exact built-in
imagegen prompt, source path and the saved Windflower example's provenance.

Both blink and simultaneous mouth/blink renders were inspected. A512px comparison
found4,180 changed pixels, all inside the two eye regions. The old draft atlas's
blink cells remain unused. Listening tilt is limited to 0.7 degrees. Two-state
mouth and eyelid motion are not phonemes or emotion inference.

The actual call dialog now supports queued speaker timing, browser output timing,
no artwork downloads while motion is off/reduced/hidden, full keyboard wrapping,
a 44px setting target and scrolling on short displays. The saved motion preference
starts off; reduced motion and hidden views stop decorative frames. Unknown
speaker metadata cannot authorize loading another person's profile.

46 focused checks, the full local call-dialog/browser contract, and the complete
frontend build pass.14 whole-project type diagnostics reproduce on the unchanged
parent. Physical-device and genuine production-call acceptance remain gates.
See CHARACTER_CALL.md for current contracts, commands and release limits.
